name: release fabric
on:
  push:
    tags:
      # Example: 1.0.0-MC1.19.x-forge
      - "[0-9]+.[0-9]+.[0-9]+-MC[0-9]+.[0-9]+.[0-9x]+-forge"

jobs:
  build:
    strategy:
      matrix:
        os: [ ubuntu-latest, windows-latest ]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17
      - name: build
        uses: gradle/gradle-build-action@v2
        with:
          gradle-version: 8.1.1
          build-root-directory: forge
          arguments: build
          cache-read-only: true
      - name: Release
        uses: docker://antonyurchenko/git-release:5.0.2 # https://github.com/marketplace/actions/git-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: forge/build/libs/*
